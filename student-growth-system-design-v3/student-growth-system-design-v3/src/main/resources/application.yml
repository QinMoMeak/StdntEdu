spring:
  application:
    name: student-growth-baseline
  profiles:
    active: local
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 270MB
      file-size-threshold: 0B
app:
  version: 0.2.0-SNAPSHOT
  ai:
    study-plan:
      executor:
        core-size: 2
        max-size: 4
        queue-capacity: 100
