# V3 修订说明

## 逻辑删除与乐观锁
仅要求业务主表具备；关系表、历史表、日志表、任务表和基础字典表不强制。

## AI 状态
任务状态使用 `REVIEW_REQUIRED`；临时题目状态使用 `PENDING_REVIEW`。两套状态语义不同。

## OpenAPI
主要 POST/PUT 必须有 requestBody；主要成功响应必须绑定 Schema；所有路径变量必须声明为 required path parameter。
