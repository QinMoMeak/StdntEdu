# AI设计
统一AiGateway能力：chat、vision、ocr、structuredOutput、testConnection。

Provider可包含豆包、通义、DeepSeek、OpenAI兼容接口和Ollama。

每次调用保存模型、Prompt版本、输入摘要、结果JSON、状态、Token、耗时和错误。

AI约束：
- 输出必须经过JSON Schema校验
- 错题识别必须人工确认
- 不得直接修改成绩、掌握度和复习阶段
- 图片只识别一次，后续优先使用文本和JSON
- API Key只通过环境变量或加密引用保存
