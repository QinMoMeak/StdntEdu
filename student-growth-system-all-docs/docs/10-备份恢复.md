# 备份恢复
类型：DATABASE、ATTACHMENT、CONFIG、FULL、PRE_RESTORE、PRE_UPGRADE。

策略：每日数据库备份保留14份；每周完整备份保留8份；手动备份不自动删除。

完整包：database.sql.gz、files/、manifest.json、checksums.sha256。

恢复必须异步执行；恢复前创建快照；恢复期间禁止普通写入；恢复后执行Flyway和一致性检查。
