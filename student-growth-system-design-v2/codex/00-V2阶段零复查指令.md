# V2阶段零复查
只读检查，不写代码。验证schema-full.sql可执行、Flyway覆盖、初始化SQL有INSERT、student_mastery和mastery_history符合算法、growth_event/growth_report/study_plan/import/export/restore/AI临时表存在、资源状态为COMPLETED、OpenAPI覆盖主要业务域。按阻塞/重要/可延后输出。
