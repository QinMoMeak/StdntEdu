spring:
  datasource:
    url: jdbc:mysql://localhost:3306/student_growth?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: student_growth
    password: student_growth
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
